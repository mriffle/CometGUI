/**
 * The adapter contract shared by every tool: ToolAdapter, ToolCommand, ToolExecutionRequest, ToolExecutionResult and their exceptions.
 *
 * <p>Filled by phase 08 (workflow engine and Comet adapter), which defines the contract the later adapters implement.</p>
 */
package org.cometgui.tools.api;
