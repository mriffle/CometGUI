/**
 * Root of the scientific tool adapters. Each adapter identifies a binary, probes its capabilities, builds its argument array and validates the outputs it claims to have produced.
 *
 * <p>Filled by phases 08 to 12, one tool per phase.</p>
 */
package org.cometgui.tools;
