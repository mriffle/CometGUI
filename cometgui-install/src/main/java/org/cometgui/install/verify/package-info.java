/**
 * Checksum verification. SHA-256 verification is mandatory before an executable is launched and a mismatch blocks execution (R-SEC-02); MD5 is recorded but is never the trust mechanism.
 *
 * <p>Filled by phase 05 (tool registry and installer).</p>
 */
package org.cometgui.install.verify;
