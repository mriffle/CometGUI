/**
 * Downloading artefacts to a temporary file, with resume, progress and the network-failure behaviour the failure-path tests require.
 *
 * <p>Filled by phase 05 (tool registry and installer).</p>
 */
package org.cometgui.install.download;
