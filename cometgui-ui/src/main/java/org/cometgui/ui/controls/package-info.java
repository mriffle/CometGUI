/**
 * Reusable controls, notably the structured parameter editors. Controls required by automated tests carry stable semantic identifiers (R-TEST-04); tests never locate them by pixel position.
 *
 * <p>Filled by phase 07 (Comet parameter editor UI).</p>
 */
package org.cometgui.ui.controls;
