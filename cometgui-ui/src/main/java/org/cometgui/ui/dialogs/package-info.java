/**
 * Dialogs and the file-chooser abstraction, which exists so that end-to-end tests can inject a selection through the same path a user's click takes.
 *
 * <p>Filled by phase 02 (application shell and navigation) and phase 05 (Tool Manager).</p>
 */
package org.cometgui.ui.dialogs;
