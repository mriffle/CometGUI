/**
 * Root of the JavaFX layer. JavaFX comes from the modules bundled in the pinned Liberica JDK, not from a Maven dependency. Controllers translate user actions into domain commands and observe state; they contain no process, hashing, download or parsing logic.
 *
 * <p>Filled by phase 02 (application shell and navigation) and by every UI-bearing phase after it.</p>
 */
package org.cometgui.ui;
