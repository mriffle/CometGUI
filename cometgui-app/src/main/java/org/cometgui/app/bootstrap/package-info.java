/**
 * Application startup: the JavaFX Application subclass, the dependency-injection wiring, and the composition root where the injectable clock, environment reader, process runner, downloader, run-ID source and hash service are chosen (R-PROC-01).
 *
 * <p>Filled by phase 02 (application shell and navigation).</p>
 */
package org.cometgui.app.bootstrap;
