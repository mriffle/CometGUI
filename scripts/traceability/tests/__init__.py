"""Unit tests for the CometGUI traceability report generator.

Run them through the gate, which also verifies that they collected anything::

    bash scripts/ci/traceability.sh

or directly::

    python -m unittest discover -s scripts/traceability/tests -t scripts
"""
