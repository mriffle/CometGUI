/**
 * The per-version option schema: which options exist for a probed Percolator version, and which were removed. Options are never offered for a version that does not have them.
 *
 * <p>Filled by phase 09 (Percolator adapter and versions).</p>
 */
package org.cometgui.params.percolator.schema;
