# CometGUI -- pinned Python tooling for the project virtualenv.
#
#   python3 -m venv .venv && .venv/bin/pip install -r requirements-dev.txt
#
# scripts/build.sh creates .venv from this file when it is missing, so that a
# clean checkout needs nothing installed on the host.  These are the exact
# versions the documentation gate (sphinx-build -n -W) is known to pass with;
# every transitive dependency is pinned deliberately, because "it built on my
# machine with whatever pip resolved today" is not reproducible.
#
# Regenerate with:  .venv/bin/pip freeze > requirements-dev.txt  (then restore
# this header).  Recorded 2026-08-29 against Python 3.11.2.
alabaster==1.0.0
babel==2.18.0
certifi==2026.7.22
charset-normalizer==3.5.1
docutils==0.22.4
idna==3.19
imagesize==2.0.1
Jinja2==3.1.6
MarkupSafe==3.0.3
packaging==26.3
Pygments==2.21.0
requests==2.34.2
roman-numerals==4.1.0
snowballstemmer==3.1.1
Sphinx==9.0.4
sphinxcontrib-applehelp==2.0.0
sphinxcontrib-devhelp==2.0.0
sphinxcontrib-htmlhelp==2.1.0
sphinxcontrib-jsmath==1.0.1
sphinxcontrib-qthelp==2.0.0
sphinxcontrib-serializinghtml==2.0.0
urllib3==2.7.0
