/**
 * Named parameter presets and the diff between a preset and the current parameter set, as previewed and applied in the editor.
 *
 * <p>Filled by phase 06 (Comet parameter model).</p>
 */
package org.cometgui.params.comet.presets;
