/**
 * Root of the workflow engine. It must be usable from tests without launching JavaFX; the UI issues commands to it and observes its state, never the other way round.
 *
 * <p>Filled by phase 08 (workflow engine and Comet adapter) and extended by phases 09 to 12.</p>
 */
package org.cometgui.workflow;
