/**
 * Observable workflow state: per-step state, the derived run state, and the transitions that are written to provenance as they happen.
 *
 * <p>Filled by phase 08 (workflow engine and Comet adapter).</p>
 */
package org.cometgui.workflow.state;
