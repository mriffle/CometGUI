/**
 * Export of filtered results, carrying the filter values actually applied so an exported table can be explained later.
 *
 * <p>Filled by phase 10 (results model and UI).</p>
 */
package org.cometgui.results.export;
