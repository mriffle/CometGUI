/**
 * q-value filtering with independent PSM and peptide thresholds. The boundary at exactly 0.01 is inclusive; a mutation that turns the comparison into a strict one must fail a test.
 *
 * <p>Filled by phase 10 (results model and UI).</p>
 */
package org.cometgui.results.filtering;
