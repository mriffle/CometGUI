/**
 * Human-readable provenance output: the reStructuredText and JSON reports, diffs between runs and the run timeline.
 *
 * <p>Filled by phase 13 (provenance UI and reports).</p>
 */
package org.cometgui.provenance.report;
