/**
 * Single-pass MD5 plus SHA-256 hashing of files of any size, with the hash cache and its invalidation policy. SHA-256 is the trust mechanism; MD5 is recorded for provenance only (R-SEC-02).
 *
 * <p>Filled by phase 04 (hashing and provenance core).</p>
 */
package org.cometgui.provenance.hashing;
