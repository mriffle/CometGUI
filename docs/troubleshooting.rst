.. _user-troubleshooting:

===============
Troubleshooting
===============

.. note::

   **Stub page -- no content yet.** Content is owned by **Phase 16 -- Documentation and Release Qualification** (``phases/PHASE-16-release.rst``), which delivers ``R-DOC-06``.

   Phase 01 created it so that the documentation tree builds strictly
   (``sphinx-build -n -W``) from the start and so that the page has a
   stable name to link to. It does not yet describe the product.

What this page will cover
=========================

``R-DOC-06`` obligation, recorded here so it is not lost: alongside
:doc:`platform_support`, this page shall state plainly which Percolator
versions support XML on which platform, so that a failed Limelight conversion
is diagnosable from the documentation. It will also cover failed downloads and
checksum mismatches, tools that will not run, searches that produce no
identifications, and where the logs are.
