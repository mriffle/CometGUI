.. _user-tool-manager:

============
Tool Manager
============

.. note::

   **Stub page -- no content yet.** Content is owned by **Phase 05 -- Tool Registry and Installer** (``phases/PHASE-05-tool-registry.rst``), which names this page in its deliverables. Phase 05's deliverable list writes the path as ``docs/user/tool_manager.rst``; the specification's recommended tree puts it at ``docs/tool_manager.rst``, which is the path used here.

   Phase 01 created it so that the documentation tree builds strictly
   (``sphinx-build -n -W``) from the start and so that the page has a
   stable name to link to. It does not yet describe the product.

What this page will cover
=========================

Installing, updating, verifying and removing the scientific tools CometGUI
manages, and reading what the installer probed about each binary after
installing it.
