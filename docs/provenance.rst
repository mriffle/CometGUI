.. _user-provenance:

==========
Provenance
==========

.. note::

   **Stub page -- no content yet.** Content is owned by **Phase 13 -- Provenance UI and Reports** (``phases/PHASE-13-provenance-ui.rst``), which names this page in its deliverables. The model beneath it is Phase 04's (``R-PROV-01``..``R-PROV-05``).

   Phase 01 created it so that the documentation tree builds strictly
   (``sphinx-build -n -W``) from the start and so that the page has a
   stable name to link to. It does not yet describe the product.

What this page will cover
=========================

The record every run keeps of itself -- tool versions, argument arrays,
inputs and outputs with MD5 and SHA-256 -- how to read it, and how to export
it as JSON or reStructuredText.
