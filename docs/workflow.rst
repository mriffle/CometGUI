.. _user-workflow:

===================
The search workflow
===================

.. note::

   **Stub page -- no content yet.** No phase document names this page individually, so it falls to Phase 16's *complete user documentation tree* deliverable (``phases/PHASE-16-release.rst``). The behaviour it describes is built in Phase 08 (workflow engine and Comet adapter) and Phase 09 (Percolator).

   Phase 01 created it so that the documentation tree builds strictly
   (``sphinx-build -n -W``) from the start and so that the page has a
   stable name to link to. It does not yet describe the product.

What this page will cover
=========================

What a run is: the stages, what happens per input file, where outputs are
written, what may be re-run, and what a run records about itself.
