.. _user-platform-support:

================
Platform support
================

.. note::

   **Stub page -- no content yet.** Content is owned by **Phase 16 -- Documentation and Release Qualification** (``phases/PHASE-16-release.rst``), which delivers ``R-DOC-06``. **Phase 05** (``phases/PHASE-05-tool-registry.rst``) names this page in its deliverables and supplies the per-platform artefact table it is built on.

   Phase 01 created it so that the documentation tree builds strictly
   (``sphinx-build -n -W``) from the start and so that the page has a
   stable name to link to. It does not yet describe the product.

What this page will cover
=========================

``R-DOC-06`` obligation, recorded here so it is not lost: this page shall
state plainly, per platform, which Percolator versions are available as
managed installs, which of them support XML, and therefore where the
Limelight path works out of the box -- rather than leaving a user to discover
it at conversion time. Phase 00's evidence for the artefact matrix is at
:doc:`feasibility/percolator-artefacts` and
:doc:`feasibility/noxml-capability`.
