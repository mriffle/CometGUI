.. _user-variable-modifications:

======================
Variable modifications
======================

.. note::

   **Stub page -- no content yet.** Content is owned by **Phase 07 -- Comet Parameter Editor UI** (``phases/PHASE-07-comet-param-ui.rst``), which names this page in its deliverables.

   Phase 01 created it so that the documentation tree builds strictly
   (``sphinx-build -n -W``) from the start and so that the page has a
   stable name to link to. It does not yet describe the product.

What this page will cover
=========================

Comet's fifteen variable-modification slots: what each field of a slot
means, how the structured editor writes them back to ``comet.params``, and
the combinations Comet rejects.
