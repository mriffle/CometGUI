.. _user-installation:

============
Installation
============

.. note::

   **Stub page -- no content yet.** No phase document names this page individually, so it falls to Phase 16's *complete user documentation tree* deliverable (``phases/PHASE-16-release.rst``). The packaged installers it will describe are produced in Phase 14 and qualified in Phase 16.

   Phase 01 created it so that the documentation tree builds strictly
   (``sphinx-build -n -W``) from the start and so that the page has a
   stable name to link to. It does not yet describe the product.

What this page will cover
=========================

How to install CometGUI itself on each tier-1 platform, and what the first
run does. CometGUI installs Comet, Percolator, PDV and the Limelight
converter by itself, so this page covers only the application: the scientific
tools are the Tool Manager's job.
