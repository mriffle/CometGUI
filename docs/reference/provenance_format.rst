.. _ref-provenance-format:

=================
Provenance format
=================

.. note::

   **Stub page -- no content yet.** Content is owned by **Phase 04 -- Hashing and Provenance Core** (``phases/PHASE-04-provenance-core.rst``), which names this page in its deliverables. It is the versioned schema; :doc:`../developer/provenance_schema` explains the model behind it.

   Phase 01 created it so that the documentation tree builds strictly
   (``sphinx-build -n -W``) from the start and so that the page has a
   stable name to link to. It does not yet describe the product.

What this page will cover
=========================

The provenance JSON schema, field by field, with its version history and the
rules for reading a record written by an older version of the application.
