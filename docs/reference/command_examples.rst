.. _ref-command-examples:

================
Command examples
================

.. note::

   **Stub page -- no content yet.** No phase document names this page individually, so it falls to Phase 16's *complete user documentation tree* deliverable (``phases/PHASE-16-release.rst``). The argument arrays it will show are produced by the adapters in Phases 08, 09, 11 and 12.

   Phase 01 created it so that the documentation tree builds strictly
   (``sphinx-build -n -W``) from the start and so that the page has a
   stable name to link to. It does not yet describe the product.

What this page will cover
=========================

The exact commands CometGUI runs, as argument arrays rather than shell
strings, so that a run can be reproduced by hand from a provenance record.
