.. _dev-e2e-harness:

==================
End-to-end harness
==================

.. note::

   **Stub page -- no content yet.** Content is owned by **Phase 14 -- GUI Automation and Packaged End-to-End Harness** (``phases/PHASE-14-e2e.rst``), which names this page in its deliverables.

   Phase 01 created it so that the documentation tree builds strictly
   (``sphinx-build -n -W``) from the start and so that the page has a
   stable name to link to. It does not yet describe the product.

What this page will cover
=========================

``FxUiDriver`` and the two harness tiers -- an in-process robot against the
assembled application, and a driver against the packaged artefact -- plus the
failure-path suite and the check that no test bridge ships (``R-TEST-06``).
