.. _dev-tool-registry:

=============
Tool registry
=============

.. note::

   **Stub page -- no content yet.** Content is owned by **Phase 05 -- Tool Registry and Installer** (``phases/PHASE-05-tool-registry.rst``), which names this page in its deliverables.

   Phase 01 created it so that the documentation tree builds strictly
   (``sphinx-build -n -W``) from the start and so that the page has a
   stable name to link to. It does not yet describe the product.

What this page will cover
=========================

The manifest format, download and checksum verification, safe extraction of
each artefact kind, atomic installation, and the three-stage probe that
decides whether an installed binary is usable.
