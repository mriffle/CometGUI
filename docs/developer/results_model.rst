.. _dev-results-model:

=============
Results model
=============

.. note::

   **Stub page -- no content yet.** Content is owned by **Phase 10 -- Results Model and UI** (``phases/PHASE-10-results.rst``), which names this page in its deliverables.

   Phase 01 created it so that the documentation tree builds strictly
   (``sphinx-build -n -W``) from the start and so that the page has a
   stable name to link to. It does not yet describe the product.

What this page will cover
=========================

The Percolator output parsers, the disk-backed index that keeps large result
sets out of heap, and the filter model -- filters are view predicates, never a
rerun.
