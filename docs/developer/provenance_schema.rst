.. _dev-provenance-schema:

=================
Provenance schema
=================

.. note::

   **Stub page -- no content yet.** Content is owned by **Phase 04 -- Hashing and Provenance Core** (``phases/PHASE-04-provenance-core.rst``), which owns ``R-PROV-01``..``R-PROV-05`` and the versioned schema at :doc:`../reference/provenance_format`. **Phase 13** (``phases/PHASE-13-provenance-ui.rst``) is required to keep this page in sync with the provenance UI.

   Phase 01 created it so that the documentation tree builds strictly
   (``sphinx-build -n -W``) from the start and so that the page has a
   stable name to link to. It does not yet describe the product.

What this page will cover
=========================

The event model, the single-pass MD5 plus SHA-256 hashing path and its
cache, schema versioning, atomic finalisation, and the redaction rule that
keeps secrets out of the record.
