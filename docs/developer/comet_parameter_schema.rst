.. _dev-comet-parameter-schema:

======================
Comet parameter schema
======================

.. note::

   **Stub page -- no content yet.** Content is owned by **Phase 06 -- Comet Parameter Model** (``phases/PHASE-06-comet-param-model.rst``), which names this page in its deliverables. It also documents the generator behind :doc:`../reference/comet_parameters_generated` (``R-DOC-04``).

   Phase 01 created it so that the documentation tree builds strictly
   (``sphinx-build -n -W``) from the start and so that the page has a
   stable name to link to. It does not yet describe the product.

What this page will cover
=========================

How the typed, versioned parameter schema is built from Comet's own ``-q``
output plus curated metadata, how it is parsed, written, validated and
migrated, and how the drift test catches a schema that no longer matches the
binary.
