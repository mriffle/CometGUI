/**
 * Tool identity, version and capability values -- what a probed binary is and what it can do. Capability is probed, never inferred from a version number.
 *
 * <p>Filled by phase 05 (tool registry and installer) and phase 09 (Percolator versions).</p>
 */
package org.cometgui.domain.tools;
