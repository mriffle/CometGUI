/**
 * Search-result values: PSM and peptide rows, q-values, learned feature weights and the filters applied to them.
 *
 * <p>Filled by phase 10 (results model and UI).</p>
 */
package org.cometgui.domain.results;
