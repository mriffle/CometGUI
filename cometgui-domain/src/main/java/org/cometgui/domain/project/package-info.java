/**
 * Project identity and layout: the user's project, its directories, its inputs and the runs it owns.
 *
 * <p>Filled by phase 08 (workflow engine and run storage).</p>
 */
package org.cometgui.domain.project;
