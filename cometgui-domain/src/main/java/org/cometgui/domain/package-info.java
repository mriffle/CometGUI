/**
 * Root of the CometGUI domain model. Plain Java values and interfaces with no JavaFX, no Maven-plugin and no I/O framework dependency, so that every layer above can be tested without a toolkit.
 *
 * <p>Sub-packages are filled by the phase named in each; this root package itself stays empty.</p>
 */
package org.cometgui.domain;
