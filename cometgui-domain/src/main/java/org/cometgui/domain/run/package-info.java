/**
 * Run values and the workflow state model: NOT_STARTED, VALIDATING, READY, RUNNING, SUCCEEDED, FAILED, CANCEL_REQUESTED, CANCELLED and SKIPPED, plus the derived overall run state.
 *
 * <p>Filled by phase 08 (workflow engine and Comet adapter).</p>
 */
package org.cometgui.domain.run;
